import React from "react";
import Nav from "./Nav";
import "../styles/Header.scss";
const Header = () => {
  return (
    <header className="header">
      Header
      <Nav />
    </header>
  );
};

export default Header;
